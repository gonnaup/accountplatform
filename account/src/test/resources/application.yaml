server:
  port: 10003
spring:
  main:
    allow-circular-references: true
  application:
    name: account_platform
  datasource:
    username: gonnaup
    password: 123456
    url: jdbc:mysql://gonnaup.com/accountplatformtest?useSSL=false
    hikari:
      maximum-pool-size: 8
  jpa:
    #    show-sql: true
    hibernate:
      ddl-auto: update
  data:
    redis:
      database: 1
      url: gonnaup.com
      password: 123456
  cache:
    type: REDIS
    redis:
      time-to-live: 30m
accountplatform:
  account:
    default:
      account-name:
        prefix: xid_
        length: 30
      avatar:
        host: http://localhost
        path: /static/
        default-file-name: default_avatar.svg